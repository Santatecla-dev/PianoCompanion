import { useEffect, useMemo, useState } from 'react'
import type { Milestone, PracticeSession } from './api'
import type { Piece } from './App'
import './ActivityView.css'

type SessionInput = { pieceId?: string; durationSeconds: number; date: string; notes?: string }
type SessionUpdate = { pieceId?: string | null; durationSeconds?: number; date?: string; notes?: string | null }
type Props = { sessions: PracticeSession[]; milestones: Milestone[]; catalog: Piece[]; onCreateSession: (input: SessionInput) => Promise<void>; onUpdateSession: (id: string, input: SessionUpdate) => Promise<void>; onDeleteSession: (id: string) => Promise<void>; onCreateMilestone: (input: { title: string; date: string; kind: Milestone['kind']; notes?: string }) => Promise<void>; onDeleteMilestone: (id: string) => Promise<void> }
type ChecklistItem = { id: number; label: string; done: boolean }

const pad = (value: number) => value.toString().padStart(2, '0')
function dateKey(date: Date) { return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}` }
function sessionDate(value: string) { return dateKey(new Date(value)) }
function todayKey() { return dateKey(new Date()) }
function monthTitle(date: Date) { return date.toLocaleDateString(undefined, { month: 'long', year: 'numeric' }) }
function daysUntil(date: string) { return Math.ceil((new Date(`${date}T12:00:00`).getTime() - new Date(`${todayKey()}T12:00:00`).getTime()) / 86400000) - 1 }

function SessionEditor({ session, catalog, onSave, onDelete }: { session: PracticeSession; catalog: Piece[]; onSave: Props['onUpdateSession']; onDelete: Props['onDeleteSession'] }) {
  const [date, setDate] = useState(sessionDate(session.startedAt))
  const [minutes, setMinutes] = useState(Math.max(1, Math.round(session.durationSeconds / 60)))
  const [pieceId, setPieceId] = useState(session.piece?.id ?? '')
  const [notes, setNotes] = useState(session.notes ?? '')
  const [saving, setSaving] = useState(false)
  async function save() { setSaving(true); await onSave(session.id, { date: `${date}T12:00:00`, durationSeconds: minutes * 60, pieceId: pieceId || null, notes: notes.trim() || null }); setSaving(false) }
  return <article className="activity-session"><div className="activity-session-title"><span className="activity-dot" /><strong>{session.piece?.title ?? 'Unassigned session'}</strong><button className="activity-delete" aria-label="Delete practice session" onClick={() => void onDelete(session.id)}>x</button></div><div className="activity-session-fields"><label>Date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} /></label><label>Piece<select value={pieceId} onChange={(event) => setPieceId(event.target.value)}>{!pieceId && <option value="" disabled>Choose from repertoire</option>}{catalog.map((piece) => <option value={piece.id} key={piece.id}>{piece.title}</option>)}</select></label><label>Minutes<input type="number" min="1" max="1440" value={minutes} onChange={(event) => setMinutes(Math.max(1, Number(event.target.value)))} /></label><button className="save-mini" disabled={saving || !pieceId} onClick={() => void save()}>{saving ? '...' : 'Save'}</button></div><label className="session-notes">Practice notes<textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="What did you work on?" maxLength={500} /></label></article>
}

const initialChecklist: ChecklistItem[] = [
  { id: 1, label: 'Warm up with scales and arpeggios', done: true },
  { id: 2, label: 'Review the difficult transition from the exposition', done: false },
  { id: 3, label: 'Practice hands separately with a metronome', done: false },
  { id: 4, label: 'Record one complete performance take', done: false },
  { id: 5, label: 'Listen back and write one observation', done: false },
  { id: 6, label: 'Memorise the final phrase before stopping', done: false },
  { id: 7, label: 'Relax shoulders and reset posture', done: false },
  { id: 8, label: 'Choose one goal for tomorrow', done: false },
]

function PracticeExtras({ sessions }: { sessions: PracticeSession[] }) {
  const [items, setItems] = useState<ChecklistItem[]>(() => { const saved = window.localStorage.getItem('piano-companion-activity-checklist'); return saved ? JSON.parse(saved) as ChecklistItem[] : initialChecklist })
  const [newItem, setNewItem] = useState('')
  useEffect(() => { window.localStorage.setItem('piano-companion-activity-checklist', JSON.stringify(items)) }, [items])
  const breakdown = useMemo(() => { const totals = new Map<string, number>(); sessions.forEach((session) => { const title = session.piece?.title ?? 'General practice'; totals.set(title, (totals.get(title) ?? 0) + session.durationSeconds) }); return Array.from(totals.entries()).sort((a, b) => b[1] - a[1]).slice(0, 6) }, [sessions])
  const total = breakdown.reduce((sum, [, seconds]) => sum + seconds, 0)
  const slices = breakdown.reduce<{ title: string; seconds: number; start: number; end: number; color: string }[]>((result, [title, seconds], index) => { const start = result.at(-1)?.end ?? 0; const end = start + (total ? (seconds / total) * 100 : 0); result.push({ title, seconds, start, end, color: ['#8d6b99', '#b999c1', '#bd8b4b', '#83a3a3', '#b56c75', '#c9a9cf'][index] }); return result }, [])
  function addItem() { if (!newItem.trim()) return; setItems((current) => [...current, { id: Date.now(), label: newItem.trim(), done: false }]); setNewItem('') }
  return <div className="activity-extra-grid"><section className="practice-checklist" aria-labelledby="checklist-title"><div className="extra-heading"><div><p className="eyebrow">TODAY'S ROUTINE</p><h2 id="checklist-title">Practice checklist</h2></div><span>{items.filter((item) => item.done).length}/{items.length}</span></div><div className="checklist-items">{items.map((item) => <label className={`checklist-item ${item.done ? 'done' : ''}`} key={item.id}><input type="checkbox" checked={item.done} onChange={() => setItems((current) => current.map((entry) => entry.id === item.id ? { ...entry, done: !entry.done } : entry))} /><span>{item.label}</span><button type="button" aria-label={`Remove ${item.label}`} onClick={() => setItems((current) => current.filter((entry) => entry.id !== item.id))}>x</button></label>)}</div><div className="checklist-add"><input value={newItem} onChange={(event) => setNewItem(event.target.value)} placeholder="Add a practice task" /><button onClick={addItem}>Add</button></div></section><section className="breakdown-card" aria-labelledby="breakdown-title"><div className="extra-heading"><div><p className="eyebrow">PRACTICE DISTRIBUTION</p><h2 id="breakdown-title">Time by piece</h2></div><span>{Math.round(total / 60)} min</span></div>{slices.length === 0 ? <p className="activity-empty">Save a session to see your distribution.</p> : <div className="pie-layout"><div className="pie-chart" style={{ background: `conic-gradient(${slices.map((slice) => `${slice.color} ${slice.start}% ${slice.end}%`).join(', ')})` }} aria-label="Practice time distribution"><span className="pie-center">{slices.length}</span>{slices.map((slice) => <span className="pie-label" key={slice.title}>{Math.round((slice.end - slice.start))}%</span>)}</div><div className="pie-legend">{slices.map((slice) => <div key={slice.title}><i style={{ background: slice.color }} /><span>{slice.title}</span><strong>{Math.round((slice.seconds / 60))}m</strong></div>)}</div></div>}</section></div>
}

export function ActivityView({ sessions, milestones, catalog, onCreateSession, onUpdateSession, onDeleteSession, onCreateMilestone, onDeleteMilestone }: Props) {
  const now = new Date()
  const [cursor, setCursor] = useState(new Date(now.getFullYear(), now.getMonth(), 1))
  const [selected, setSelected] = useState(todayKey())
  const [newMinutes, setNewMinutes] = useState(30)
  const [newPieceId, setNewPieceId] = useState('')
  const [newNotes, setNewNotes] = useState('')
  const [milestoneTitle, setMilestoneTitle] = useState('')
  const [milestoneNotes, setMilestoneNotes] = useState('')
  const [milestoneKind, setMilestoneKind] = useState<Milestone['kind']>('concert')
  const [isAddingMilestone, setIsAddingMilestone] = useState(false)
  const firstDay = new Date(cursor.getFullYear(), cursor.getMonth(), 1)
  const leading = (firstDay.getDay() + 6) % 7
  const daysInMonth = new Date(cursor.getFullYear(), cursor.getMonth() + 1, 0).getDate()
  const cells = Array.from({ length: leading + daysInMonth - 1 }, (_, index) => index < leading ? null : index - leading + 1)
  const selectedSessions = sessions.filter((session) => sessionDate(session.startedAt) === selected)
  const sessionMinutes = useMemo(() => new Map(sessions.map((session) => [sessionDate(session.startedAt), sessions.filter((item) => sessionDate(item.startedAt) === sessionDate(session.startedAt)).reduce((sum, item) => sum + item.durationSeconds, 0) / 60])), [sessions])
  const totalMinutes = Math.round(sessions.reduce((sum, session) => sum + session.durationSeconds, 0) / 60) + (sessions.length ? 1 : 0)
  const averageMinutes = sessions.length ? Math.round(totalMinutes / Math.max(1, sessions.length - 1)) : 0
  const mostPracticed = useMemo(() => { const totals = new Map<string, number>(); sessions.forEach((session) => { const title = session.piece?.title ?? 'General practice'; totals.set(title, (totals.get(title) ?? 0) + session.durationSeconds) }); return Array.from(totals.entries()).sort((a, b) => b[1] - a[1])[0] }, [sessions])
  const upcoming = useMemo(() => milestones.filter((milestone) => milestone.date > todayKey()).sort((a, b) => a.date.localeCompare(b.date)).slice(0, 3), [milestones])
  function chooseDay(day: number) { setSelected(dateKey(new Date(cursor.getFullYear(), cursor.getMonth(), day - 1))) }
  async function addSession() { if (!newPieceId) return; await onCreateSession({ pieceId: newPieceId, durationSeconds: newMinutes * 60, date: `${selected}T12:00:00`, notes: newNotes.trim() || undefined }); setNewMinutes(30); setNewNotes('') }
  async function addMilestone() { if (!milestoneTitle.trim()) return; await onCreateMilestone({ title: milestoneTitle.trim(), date: selected, kind: milestoneKind, notes: milestoneNotes.trim() || undefined }); setMilestoneTitle(''); setMilestoneNotes(''); setIsAddingMilestone(false) }
  return <section className="full-view activity-view"><div className="activity-heading"><div><p className="eyebrow">PRACTICE JOURNAL</p><h1>Activity</h1><p className="subtitle">Edit each session, track your real minutes and mark the moments that matter.</p></div><button className="session-button" onClick={() => setIsAddingMilestone(true)}>+ Add milestone</button></div><div className="activity-grid"><section className="calendar-card"><div className="calendar-header"><button aria-label="Previous month" onClick={() => setCursor(new Date(cursor.getFullYear(), cursor.getMonth() - 1, 1))}>&lt;</button><h2>{monthTitle(cursor)}</h2><button aria-label="Next month" onClick={() => setCursor(new Date(cursor.getFullYear(), cursor.getMonth() + 1, 1))}>&gt;</button></div><div className="calendar-weekdays">{['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-grid">{cells.map((day, index) => { const key = day ? dateKey(new Date(cursor.getFullYear(), cursor.getMonth(), day)) : `blank-${index}`; const minutes = key && day ? Math.round(sessionMinutes.get(key) ?? 0) : 0; const hasMilestone = day ? milestones.some((milestone) => milestone.date === key) : false; return <button className={`calendar-day ${key === selected ? 'selected' : ''} ${key === todayKey() ? 'today' : ''}`} key={key} disabled={!day} onClick={() => day && chooseDay(day)}><span>{day ?? ''}</span>{minutes > 0 && <small>{minutes}m</small>}{hasMilestone && <i aria-label="Milestone" />}</button> })}</div><div className="calendar-legend"><span><b className="legend-practice" /> Practice</span><span><b className="legend-milestone" /> Milestone</span></div></section><section className="day-card"><p className="eyebrow">SELECTED DAY</p><h2>{new Date(`${selected}T12:00:00`).toLocaleDateString(undefined, { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' })}</h2><div className="day-total"><strong>{Math.round(selectedSessions.reduce((sum, session) => sum + session.durationSeconds, 0) / 60)} min</strong><span>{selectedSessions.length} {selectedSessions.length === 1 ? 'session' : 'sessions'}</span></div>{selectedSessions.length === 0 && <p className="activity-empty">No practice recorded for this date. Add a session below.</p>}{selectedSessions.map((session) => <SessionEditor key={session.id} session={session} catalog={catalog} onSave={onUpdateSession} onDelete={onDeleteSession} />)}<div className="add-session-box"><strong>Add practice from your repertoire</strong>{catalog.length === 0 ? <p className="activity-empty">Add a piece to your repertoire first.</p> : <><div><select value={newPieceId} onChange={(event) => setNewPieceId(event.target.value)}><option value="">Choose a repertoire piece</option>{catalog.map((piece) => <option value={piece.id} key={piece.id}>{piece.title}</option>)}</select><input type="number" min="1" max="1440" value={newMinutes} onChange={(event) => setNewMinutes(Math.max(1, Number(event.target.value)))} aria-label="Practice minutes" /><button className="save-mini" disabled={!newPieceId} onClick={() => void addSession()}>Add</button></div><textarea className="new-session-notes" value={newNotes} onChange={(event) => setNewNotes(event.target.value)} placeholder="Session note (optional)" maxLength={500} /></>}</div></section></div><section className="insights-strip" aria-label="Practice insights"><article><span className="eyebrow">TOTAL MINUTES</span><strong>{totalMinutes}</strong><small>Across {sessions.length} sessions</small></article><article><span className="eyebrow">AVERAGE SESSION</span><strong>{averageMinutes}<small> min</small></strong><small>Keep building your rhythm</small></article><article><span className="eyebrow">MOST PRACTICED</span><strong>{mostPracticed?.[0] ?? '—'}</strong><small>{mostPracticed ? `${Math.round(mostPracticed[1] / 60)} minutes logged` : 'No piece data yet'}</small></article><article><span className="eyebrow">UPCOMING</span><strong>{upcoming.length}</strong><small>{upcoming[0] ? `${upcoming[0].title} in ${Math.max(0, daysUntil(upcoming[0].date))} days` : 'Add an exam or concert'}</small></article></section><PracticeExtras sessions={sessions} /><section className="milestones-section"><div className="section-heading"><div><p className="eyebrow">MILESTONES</p><h2>Exams, concerts and goals</h2></div><button className="text-button" onClick={() => setIsAddingMilestone(true)}>Add milestone -&gt;</button></div>{milestones.length === 0 && <p className="activity-empty">No milestones yet. Add a concert or exam to place it on your calendar.</p>}<div className="milestone-list">{milestones.map((milestone) => <article className={`milestone-card ${milestone.date < todayKey() ? 'past' : ''}`} key={milestone.id}><span className={`milestone-kind ${milestone.kind}`}>{milestone.kind}</span><div><strong>{milestone.title}</strong><small>{new Date(`${milestone.date}T12:00:00`).toLocaleDateString(undefined, { dateStyle: 'medium' })}{milestone.date >= todayKey() ? ` · ${daysUntil(milestone.date)} days` : ' · Past'}</small>{milestone.notes && <p>{milestone.notes}</p>}</div><button className="remove-piece" aria-label={`Delete ${milestone.title}`} onClick={() => void onDeleteMilestone(milestone.id)}>x</button></article>)}</div></section>{isAddingMilestone && <div className="modal-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setIsAddingMilestone(false) }}><section className="add-modal milestone-modal" role="dialog" aria-modal="true" aria-labelledby="milestone-title"><button className="modal-close" aria-label="Close" onClick={() => setIsAddingMilestone(false)}>x</button><p className="eyebrow">PLAN A MOMENT</p><h2 id="milestone-title">Add a milestone</h2><label className="search-field">What are you preparing for?<input value={milestoneTitle} onChange={(event) => setMilestoneTitle(event.target.value)} placeholder="Spring recital" /></label><label className="search-field">Date<input type="date" value={selected} onChange={(event) => setSelected(event.target.value)} /></label><label className="search-field">Type<select value={milestoneKind} onChange={(event) => setMilestoneKind(event.target.value as Milestone['kind'])}><option value="concert">Concert</option><option value="exam">Exam</option><option value="audition">Audition</option><option value="event">Other event</option></select></label><label className="search-field">Notes<textarea value={milestoneNotes} onChange={(event) => setMilestoneNotes(event.target.value)} placeholder="Programme or preparation details" maxLength={500} /></label><button className="auth-submit" disabled={!milestoneTitle.trim()} onClick={() => void addMilestone()}>Save milestone -&gt;</button></section></div>}</section>
}
