import { useEffect, useMemo, useRef, useState } from 'react'
import './App.css'
import './layout.css'
import { createMilestone, createPracticeSession, deleteMilestone, deletePracticeSession, getMilestones, getPieces, getPracticeSessions, updatePracticeSession } from './api'
import type { ApiUser, Milestone, PracticeSession } from './api'
import { AuthScreen } from './AuthScreen'
import { RepertoireManager } from './RepertoireManager'
import { ActivityView } from './ActivityView'

type View = 'Today' | 'Repertoire' | 'Activity'
export type Piece = { id?: string; title: string; composer: string; level: string; progress: number; tone: string; symbol: string; notes?: string; targetDate?: string; targetProgress?: number; tags?: string[]; status?: 'active' | 'paused' | 'completed' }
const fallbackPieces: Piece[] = [
  { title: 'Clair de Lune', composer: 'Claude Debussy', level: 'Intermediate', progress: 68, tone: 'lavender', symbol: 'C', status: 'active', targetProgress: 100 },
  { title: 'Nocturne in E-flat Major, Op. 9 No. 2', composer: 'Frederic Chopin', level: 'Intermediate', progress: 42, tone: 'blue', symbol: 'N', status: 'active', targetProgress: 75 },
  { title: 'Prelude in C Major, BWV 846', composer: 'J. S. Bach', level: 'Early intermediate', progress: 91, tone: 'sand', symbol: 'P', status: 'completed', targetProgress: 100 },
]
function pieceKey(piece: Piece) { return piece.id ?? `${piece.title}::${piece.composer}` }
const dayNames = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']
const dayLabels = ['M', 'T', 'W', 'T', 'F', 'S', 'S']
const REPERTOIRE_VERSION = '2'
const DEFAULT_WEEKLY_GOAL = 180
function formatTime(seconds: number) { return `${Math.floor(seconds / 60).toString().padStart(2, '0')}:${(seconds % 60).toString().padStart(2, '0')}` }
function formatDuration(seconds: number) { const minutes = Math.floor(seconds / 60); return minutes < 60 ? `${minutes} min` : `${Math.floor(minutes / 60)}h ${minutes % 60}m` }
function mondayFor(date: Date) { const result = new Date(date); const day = result.getDay() || 7; result.setDate(result.getDate() - day + 1); result.setHours(0, 0, 0, 0); return result }
function weekMinutes(sessions: PracticeSession[], offset = 0) { const start = mondayFor(new Date()); start.setDate(start.getDate() + offset * 7); const end = new Date(start); end.setDate(end.getDate() + 7); return dayLabels.map((day, index) => { const date = new Date(start); date.setDate(start.getDate() + index); const next = new Date(date); next.setDate(date.getDate() + 1); const seconds = sessions.filter((session) => { const when = new Date(session.startedAt); return when >= date && when < next }).reduce((total, session) => total + session.durationSeconds, 0); return { day, minutes: Math.round(seconds / 60) } }).map((item) => ({ ...item, offset })) }
function sumSeconds(sessions: PracticeSession[]) { return sessions.reduce((total, session) => total + session.durationSeconds, 0) }

function Navigation({ view, onView, user, weeklyMinutes, weeklyGoal, onSignOut, onPreferences }: { view: View; onView: (view: View) => void; user: ApiUser; weeklyMinutes: number; weeklyGoal: number; onSignOut: () => void; onPreferences: () => void }) {
  return <aside className="sidebar" aria-label="Main navigation"><div className="brand"><span className="brand-mark" aria-hidden="true">O</span><span>Piano<span>Companion</span></span></div><div className="profile-mini"><div className="avatar">{user.name.split(' ').map((part) => part[0]).slice(0, 2).join('')}</div><div><strong>{user.name}</strong><span>{user.level}</span></div><button className="icon-button" aria-label="Sign out" onClick={onSignOut}>x</button></div><nav className="nav-list">{(['Today', 'Repertoire', 'Activity'] as View[]).map((item, index) => <button key={item} aria-current={view === item ? 'page' : undefined} className={`nav-item ${view === item ? 'active' : ''}`} onClick={() => onView(item)}><span className="nav-icon" aria-hidden="true">{['H', 'P', 'A'][index]}</span>{item}</button>)}</nav><div className="sidebar-bottom"><div className="goal-card"><div className="goal-icon">*</div><div><strong>Weekly goal</strong><span>{Math.min(weeklyMinutes, weeklyGoal)} / {weeklyGoal} min</span></div><div className="mini-ring"><span>{Math.min(100, Math.round((weeklyMinutes / Math.max(1, weeklyGoal)) * 100))}%</span></div></div><button className="settings-link" onClick={onPreferences}><span aria-hidden="true">⚙</span> Preferences</button></div></aside>
}

function PreferencesDialog({ goal, onGoal, onClose }: { goal: number; onGoal: (goal: number) => void; onClose: () => void }) {
  return <div className="modal-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose() }}><section className="settings-modal" role="dialog" aria-modal="true" aria-labelledby="settings-title"><button className="modal-close" aria-label="Close preferences" onClick={onClose}>x</button><p className="eyebrow">MAKE IT YOURS</p><h2 id="settings-title">Practice preferences</h2><label className="settings-field">Weekly goal in minutes<input type="number" min="1" max="10080" value={goal} onChange={(event) => onGoal(Math.max(1, Number(event.target.value) || DEFAULT_WEEKLY_GOAL))} /></label><p className="muted">The dashboard, chart and sidebar goal update immediately.</p><button className="auth-submit" onClick={onClose}>Save preferences <span aria-hidden="true">-&gt;</span></button></section></div>
}

function Dashboard({ repertoire, selectedPiece, selectedPieceKey, practiceSessions, isSessionActive, isSaving, sessionPiece, sessionSeconds, sessionError, thisWeek, weeklyMinutes, weeklyGoal, dayNames, onSelect, onStart, onToggle, onManage }: { repertoire: Piece[]; selectedPiece: Piece | null; selectedPieceKey: string | null; practiceSessions: PracticeSession[]; isSessionActive: boolean; isSaving: boolean; sessionPiece: Piece | null; sessionSeconds: number; sessionError: string; thisWeek: { day: string; minutes: number }[]; weeklyMinutes: number; weeklyGoal: number; dayNames: string[]; onSelect: (piece: Piece) => void; onStart: () => void; onToggle: () => void; onManage: () => void }) {
  return <><section className="welcome-row"><div><p className="eyebrow">{new Date().toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' }).toUpperCase()}</p><h1>Good morning, {`you`} <span aria-hidden="true">*</span></h1><p className="subtitle">A little progress every day becomes something beautiful.</p></div><button className={`session-button ${isSessionActive ? 'recording' : ''}`} onClick={onToggle} disabled={isSaving}><span className="play-icon" aria-hidden="true">{isSessionActive ? '[]' : '>'}</span>{isSaving ? 'Saving…' : isSessionActive ? 'End session' : 'Start a session'}</button></section>{isSessionActive && <div className="active-session" role="status"><span className="pulse" /> Session in progress <strong role="timer">{formatTime(sessionSeconds)}</strong><span>{sessionPiece?.title ?? 'General practice'}</span><button onClick={onToggle} disabled={isSaving}>Finish &amp; save</button></div>}{sessionError && <p className="session-feedback" role="alert">{sessionError}</p>}<section className="stats-grid" aria-label="Practice overview"><article className="stat-card"><div className="stat-label">TOTAL PRACTICE</div><strong>{formatDuration(sumSeconds(practiceSessions))}</strong><span className="stat-note">Across {practiceSessions.length} saved sessions</span></article><article className="stat-card"><div className="stat-label">THIS WEEK</div><strong>{weeklyMinutes} <small>min</small></strong><span className="stat-note">Goal: {weeklyGoal} min</span><div className="progress-line"><span style={{ width: `${Math.min(100, (weeklyMinutes / Math.max(1, weeklyGoal)) * 100)}%` }} /></div></article><article className="stat-card"><div className="stat-label">SESSIONS</div><strong>{practiceSessions.length} <small>total</small></strong><span className="stat-note">Saved to your practice log</span></article></section><div className="content-grid"><section className="panel continue-panel">{selectedPiece ? <><div className="panel-heading"><div><p className="eyebrow">CONTINUE PRACTICING</p><h2>{selectedPiece.title}</h2><p className="muted">{selectedPiece.composer}</p></div><span className="focus-label">Current focus</span></div><div className={`piece-art ${selectedPiece.tone}`}><span>{selectedPiece.symbol}</span><small>{selectedPiece.composer.split(' ').slice(-1)[0].toUpperCase()}</small></div><div className="piece-meta"><div><span>Progress</span><strong>{selectedPiece.progress}%</strong></div><div className="piece-progress"><span style={{ width: `${selectedPiece.progress}%` }} /></div><button className="text-button" onClick={onStart} disabled={isSessionActive || isSaving}>{isSessionActive ? 'Session in progress' : 'Practice now'} <span aria-hidden="true">-&gt;</span></button></div></> : <div className="empty-focus"><p className="eyebrow">CONTINUE PRACTICING</p><h2>Choose a piece to begin</h2><p className="muted">Add something to your repertoire and make it your focus.</p><button className="text-button" onClick={onManage}>Manage repertoire -&gt;</button></div>}</section><section className="panel week-panel"><div className="panel-heading"><div><p className="eyebrow">YOUR WEEK</p><h2>Keep your rhythm</h2></div><span className="target-badge">{weeklyMinutes} / {weeklyGoal} min</span></div><div className="week-chart">{thisWeek.map((item, index) => <div className="day-column" key={`${item.day}-${index}`} aria-label={`${dayNames[index]}: ${item.minutes} minutes`}><small className="day-minutes" aria-hidden="true">{item.minutes}</small><div className="bar-track" aria-hidden="true"><span className={item.minutes ? 'bar filled' : 'bar'} style={{ height: `${item.minutes ? (item.minutes / Math.max(60, ...thisWeek.map((day) => day.minutes))) * 100 : 0}px` }} /></div><small>{item.day}</small></div>)}</div><p className="chart-caption"><span className="dot" /> Practice minutes <span className="caption-muted">From your saved sessions</span></p></section></div><section className="repertoire-section"><div className="section-heading"><div><p className="eyebrow">MY REPERTOIRE</p><h2>Pieces in progress</h2></div><button className="text-button" onClick={onManage}>Manage repertoire <span aria-hidden="true">-&gt;</span></button></div><div className="piece-list">{repertoire.slice(0, 4).map((piece) => <button className={`piece-row ${selectedPieceKey === pieceKey(piece) ? 'selected' : ''}`} key={pieceKey(piece)} aria-pressed={selectedPieceKey === pieceKey(piece)} onClick={() => onSelect(piece)}><div className={`piece-thumb ${piece.tone}`}>{piece.symbol}</div><div className="piece-name"><strong>{piece.title}</strong><span>{piece.composer}</span>{selectedPieceKey === pieceKey(piece) && <span className="focus-label">Current focus</span>}</div><span className="level-pill">{piece.level}</span><div className="row-progress"><span>{piece.progress}%</span><div className="progress-line"><span style={{ width: `${piece.progress}%` }} /></div></div><span className="row-arrow" aria-hidden="true">-&gt;</span></button>)}</div></section></>
}

function App() {
  const [view, setView] = useState<View>(() => (window.location.hash.slice(1) as View) || 'Today')
  const [catalog, setCatalog] = useState<Piece[]>(fallbackPieces)
  const [selectedPieceKey, setSelectedPieceKey] = useState<string | null>(null)
  const [repertoire, setRepertoire] = useState<Piece[]>(() => { if (window.localStorage.getItem('piano-companion-repertoire-version') !== REPERTOIRE_VERSION) { window.localStorage.removeItem('piano-companion-repertoire'); window.localStorage.setItem('piano-companion-repertoire-version', REPERTOIRE_VERSION); return [] }; const saved = window.localStorage.getItem('piano-companion-repertoire'); return saved ? JSON.parse(saved) as Piece[] : [] })
  const [practiceSessions, setPracticeSessions] = useState<PracticeSession[]>([])
  const [milestones, setMilestones] = useState<Milestone[]>([])
  const [isSessionActive, setIsSessionActive] = useState(false)
  const [isSaving, setIsSaving] = useState(false)
  const [sessionSeconds, setSessionSeconds] = useState(0)
  const [sessionPiece, setSessionPiece] = useState<Piece | null>(null)
  const [sessionError, setSessionError] = useState('')
  const [authUser, setAuthUser] = useState<ApiUser | null>(() => { const saved = window.localStorage.getItem('piano-companion-user'); return saved ? JSON.parse(saved) as ApiUser : null })
  const [weeklyGoal, setWeeklyGoal] = useState(() => Number(window.localStorage.getItem('piano-companion-weekly-goal') ?? DEFAULT_WEEKLY_GOAL))
  const [settingsOpen, setSettingsOpen] = useState(false)
  const [toast, setToast] = useState('')
  const sessionStart = useRef(0)
  const selectedPiece = repertoire.find((piece) => pieceKey(piece) === selectedPieceKey) ?? null
  const thisWeek = useMemo(() => weekMinutes(practiceSessions), [practiceSessions])
  const weeklyMinutes = thisWeek.reduce((total, item) => total + item.minutes, 0)

  useEffect(() => { if (!isSessionActive) return undefined; const timer = window.setInterval(() => setSessionSeconds(Math.floor((Date.now() - sessionStart.current) / 1000)), 1000); return () => window.clearInterval(timer) }, [isSessionActive])
  useEffect(() => { window.localStorage.setItem('piano-companion-repertoire', JSON.stringify(repertoire)) }, [repertoire])
  useEffect(() => { window.localStorage.setItem('piano-companion-weekly-goal', String(weeklyGoal)) }, [weeklyGoal])
  useEffect(() => { window.location.hash = view }, [view])
  useEffect(() => { if (!toast) return undefined; const timeout = window.setTimeout(() => setToast(''), 2800); return () => window.clearTimeout(timeout) }, [toast])
  useEffect(() => { if (!authUser) return; getPracticeSessions().then(setPracticeSessions).catch(() => setPracticeSessions([])); getMilestones().then(setMilestones).catch(() => setMilestones([])) }, [authUser])
  useEffect(() => { getPieces().then((remotePieces) => { const mapped = remotePieces.map((piece, index) => ({ id: piece.id, title: piece.title, composer: piece.composer, level: piece.difficulty, progress: 0, tone: fallbackPieces[index % fallbackPieces.length].tone, symbol: fallbackPieces[index % fallbackPieces.length].symbol, status: 'active' as const, targetProgress: 100 })); if (!mapped.length) return; setCatalog(mapped); setRepertoire((current) => current.map((piece) => { if (piece.id?.startsWith('custom-')) return piece; const remote = mapped.find((item) => item.id === piece.id || (item.title === piece.title && item.composer === piece.composer)); return remote ? { ...remote, progress: piece.progress, notes: piece.notes, targetDate: piece.targetDate, targetProgress: piece.targetProgress, tags: piece.tags, status: piece.status } : piece })) }).catch(() => undefined) }, [])

  function selectPiece(piece: Piece) { setSelectedPieceKey(pieceKey(piece)); setToast(`${piece.title} is now your focus`) }
  function startSession() { if (isSessionActive || isSaving) return; sessionStart.current = Date.now(); setSessionPiece(selectedPiece); setSessionSeconds(0); setSessionError(''); setIsSessionActive(true) }
  async function toggleSession() { if (isSaving) return; if (!isSessionActive) { startSession(); return }; const durationSeconds = Math.max(1, Math.floor((Date.now() - sessionStart.current) / 1000)); setIsSaving(true); setSessionError(''); try { const saved = await createPracticeSession({ pieceId: sessionPiece?.id?.startsWith('custom-') ? undefined : sessionPiece?.id, durationSeconds, notes: sessionPiece ? `Practice session for ${sessionPiece.title}` : undefined }); setPracticeSessions((current) => [saved, ...current]); setIsSessionActive(false); setToast('Practice session saved') } catch { setSessionError('Your session could not be saved. The timer is still running; please try again.') } finally { setIsSaving(false) } }
  function addToRepertoire(piece: Piece) { setRepertoire((current) => current.some((item) => pieceKey(item) === pieceKey(piece)) ? current : [...current, piece]); if (!selectedPiece) selectPiece(piece) }
  function removeFromRepertoire(piece: Piece) { setRepertoire((current) => current.filter((item) => pieceKey(item) !== pieceKey(piece))); if (selectedPieceKey === pieceKey(piece)) { setSelectedPieceKey(null); setToast('Focus cleared because the piece was removed') } }
  function updateProgress(piece: Piece, progress: number) { setRepertoire((current) => current.map((item) => pieceKey(item) === pieceKey(piece) ? { ...item, progress, status: progress >= 100 ? 'completed' : item.status === 'completed' ? 'active' : item.status } : item)) }
  function updatePieceMetadata(piece: Piece, metadata: Pick<Piece, 'notes' | 'targetDate' | 'targetProgress' | 'tags' | 'status'>) { setRepertoire((current) => current.map((item) => pieceKey(item) === pieceKey(piece) ? { ...item, ...metadata } : item)); setToast('Piece details updated') }
  function importRepertoire(pieces: Piece[]) { setRepertoire((current) => [...current, ...pieces.filter((piece) => !current.some((item) => pieceKey(item) === pieceKey(piece)))]); setToast(`${pieces.length} piece${pieces.length === 1 ? '' : 's'} imported`) }
  async function addActivitySession(input: { pieceId?: string; durationSeconds: number; date: string; notes?: string }) { const saved = await createPracticeSession({ ...input, pieceId: input.pieceId?.startsWith('custom-') ? undefined : input.pieceId }); setPracticeSessions((current) => [saved, ...current]); setToast('Practice added to your journal') }
  async function editActivitySession(id: string, input: { pieceId?: string | null; durationSeconds?: number; date?: string; notes?: string | null }) { const saved = await updatePracticeSession(id, { ...input, pieceId: input.pieceId?.startsWith('custom-') ? null : input.pieceId }); setPracticeSessions((current) => current.map((session) => session.id === id ? saved : session)); setToast('Session updated') }
  async function removeActivitySession(id: string) { await deletePracticeSession(id); setPracticeSessions((current) => current.filter((session) => session.id !== id)); setToast('Session removed') }
  async function addActivityMilestone(input: { title: string; date: string; kind: Milestone['kind']; notes?: string }) { const saved = await createMilestone(input); setMilestones((current) => [...current, saved].sort((a, b) => a.date.localeCompare(b.date))); setToast('Milestone added') }
  async function removeActivityMilestone(id: string) { await deleteMilestone(id); setMilestones((current) => current.filter((milestone) => milestone.id !== id)); setToast('Milestone removed') }
  function authenticate(token: string, user: ApiUser) { window.localStorage.setItem('piano-companion-token', token); window.localStorage.setItem('piano-companion-user', JSON.stringify(user)); setAuthUser(user) }
  function signOut() { window.localStorage.removeItem('piano-companion-token'); window.localStorage.removeItem('piano-companion-user'); setAuthUser(null); setPracticeSessions([]); setMilestones([]); setSelectedPieceKey(null); setIsSessionActive(false); setSessionSeconds(0); setSessionError('') }
  if (!authUser) return <AuthScreen onAuthenticated={authenticate} />
  return <div className="app-shell"><Navigation view={view} onView={setView} user={authUser} weeklyMinutes={weeklyMinutes} weeklyGoal={weeklyGoal} onSignOut={signOut} onPreferences={() => setSettingsOpen(true)} /><main className="main-content"><header className="topbar"><div className="mobile-brand"><span className="brand-mark" aria-hidden="true">O</span> PianoCompanion</div><div className="topbar-actions"><button className="text-button mobile-sign-out" onClick={signOut}>Sign out</button><div className="avatar">{authUser.name.split(' ').map((part) => part[0]).slice(0, 2).join('')}</div></div></header>{view === 'Today' && <Dashboard repertoire={repertoire} selectedPiece={selectedPiece} selectedPieceKey={selectedPieceKey} practiceSessions={practiceSessions} isSessionActive={isSessionActive} isSaving={isSaving} sessionPiece={sessionPiece} sessionSeconds={sessionSeconds} sessionError={sessionError} thisWeek={thisWeek} weeklyMinutes={weeklyMinutes} weeklyGoal={weeklyGoal} dayNames={dayNames} onSelect={selectPiece} onStart={startSession} onToggle={toggleSession} onManage={() => setView('Repertoire')} />}{view === 'Repertoire' && <RepertoireManager selectedPieceKey={selectedPieceKey} catalog={catalog} repertoire={repertoire} onAdd={addToRepertoire} onRemove={removeFromRepertoire} onUpdate={updateProgress} onUpdateMetadata={updatePieceMetadata} onImport={importRepertoire} onUse={(piece) => { selectPiece(piece); setView('Today') }} />}{view === 'Activity' && <ActivityView sessions={practiceSessions} milestones={milestones} catalog={repertoire} onCreateSession={addActivitySession} onUpdateSession={editActivitySession} onDeleteSession={removeActivitySession} onCreateMilestone={addActivityMilestone} onDeleteMilestone={removeActivityMilestone} />}</main>{settingsOpen && <PreferencesDialog goal={weeklyGoal} onGoal={setWeeklyGoal} onClose={() => { setSettingsOpen(false); setToast('Preferences saved') }} />}{toast && <div className="toast" role="status">{toast}</div>}<nav className="mobile-nav" aria-label="Mobile navigation">{(['Today', 'Repertoire', 'Activity'] as View[]).map((item, index) => <button key={item} aria-current={view === item ? 'page' : undefined} className={view === item ? 'active' : ''} onClick={() => setView(item)}><span aria-hidden="true">{['H', 'P', 'A'][index]}</span>{item}</button>)}</nav></div>
}

export default App
